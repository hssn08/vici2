<?xml version="1.0"?>
<!--
  vici2 FreeSWITCH master XML loader.
  F03 ships the canonical layout per PLAN §15. Sections:
    - vars.xml         : global variables (rendered from .tmpl by entrypoint)
    - configuration    : autoload_configs/*.xml
    - dialplan         : default and public contexts
    - directory        : F05 populates default/*.xml per agent
  Sofia profiles are pulled in by autoload_configs/sofia.conf.xml.
-->
<document type="freeswitch/xml">

  <X-PRE-PROCESS cmd="include" data="vars.xml"/>

  <section name="configuration" description="Various Configuration">
    <X-PRE-PROCESS cmd="include" data="autoload_configs/*.xml"/>
  </section>

  <section name="dialplan" description="Regex/XML Dialplan">
    <X-PRE-PROCESS cmd="include" data="dialplan/*.xml"/>
  </section>

  <section name="directory" description="User Directory">
    <X-PRE-PROCESS cmd="include" data="directory/*.xml"/>
  </section>

</document>
