<?xml version="1.0"?>
<!--
  Internal profile — F03 PLAN §2.1. Port 5060 UDP+TCP.
  Hardphones and future supervisor desk phones. Digest auth from the static
  XML directory (Phase 1; F05 Phase 2 may switch to xml_curl directory binding).
-->
<profile name="internal">
  <aliases/>
  <gateways>
    <X-PRE-PROCESS cmd="include" data="internal/*.xml"/>
  </gateways>
  <domains>
    <domain name="all" alias="true" parse="false"/>
  </domains>
  <settings>
    <param name="debug" value="0"/>
    <param name="sip-trace" value="no"/>
    <param name="sip-capture" value="no"/>
    <param name="watchdog-enabled" value="no"/>
    <param name="log-auth-failures" value="true"/>
    <param name="forward-unsolicited-mwi-notify" value="false"/>
    <param name="context" value="default"/>

    <!-- Bindings -->
    <param name="rfc2833-pt" value="101"/>
    <param name="sip-port" value="$${internal_sip_port}"/>
    <param name="dialplan" value="XML"/>
    <param name="dtmf-duration" value="2000"/>
    <param name="inbound-codec-prefs" value="$${internal_codec_prefs}"/>
    <param name="outbound-codec-prefs" value="$${internal_codec_prefs}"/>
    <param name="rtp-timer-name" value="soft"/>

    <!-- Auth -->
    <param name="auth-calls" value="true"/>
    <param name="accept-blind-reg" value="false"/>
    <param name="accept-blind-auth" value="false"/>
    <param name="auth-all-packets" value="false"/>
    <param name="nonce-ttl" value="60"/>
    <param name="manage-presence" value="false"/>

    <!-- NAT / addressing -->
    <param name="ext-rtp-ip" value="$${external_rtp_ip}"/>
    <param name="ext-sip-ip" value="$${external_sip_ip}"/>
    <param name="rtp-ip" value="$${local_ip_v4}"/>
    <param name="sip-ip" value="$${local_ip_v4}"/>
    <param name="apply-nat-acl" value="nat.auto"/>
    <param name="local-network-acl" value="localnet.auto"/>
    <param name="apply-inbound-acl" value="domestic"/>

    <!-- Media -->
    <param name="media-option" value="resume-media-on-hold"/>
    <param name="dtmf-type" value="rfc2833"/>
    <param name="enable-3pcc" value="proxy"/>
    <param name="rtp-secure-media" value="optional"/>
    <param name="hold-music" value="local_stream://moh"/>

    <!-- Misc -->
    <param name="presence-hosts" value="$${domain},$${local_ip_v4}"/>
    <param name="presence-privacy" value="false"/>
    <param name="record-template" value="$${recordings_dir}/${vici2_tenant_id}/${strftime(%Y/%m/%d)}/${vici2_campaign_id}_${vici2_lead_id}_${uuid}.wav"/>
    <param name="record-path" value="$${recordings_dir}"/>
    <param name="challenge-realm" value="auto_from"/>
  </settings>
</profile>
