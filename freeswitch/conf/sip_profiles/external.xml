<?xml version="1.0"?>
<!--
  External profile — F03 PLAN §2.3. Ports 5080 UDP+TCP.
  Carrier gateways (BYOC). Carriers authenticate via IP ACL (`carriers` —
  T02 populates) or per-gateway register.
  Inbound carrier traffic lands in the `public` dialplan; `99_drop_unauthenticated.xml`
  is the catchall. DID-routing files numbered 10_*…89_*.xml belong to I02.
-->
<profile name="external">
  <aliases/>
  <gateways>
    <X-PRE-PROCESS cmd="include" data="external/*.xml"/>
  </gateways>
  <domains/>
  <settings>
    <param name="debug" value="0"/>
    <param name="context" value="public"/>
    <param name="dialplan" value="XML"/>
    <param name="rfc2833-pt" value="101"/>
    <param name="sip-port" value="$${external_sip_port}"/>

    <!-- Auth via IP ACL or per-gateway register (no digest by default) -->
    <param name="auth-calls" value="false"/>
    <param name="accept-blind-auth" value="false"/>
    <param name="apply-inbound-acl" value="carriers"/>

    <param name="inbound-codec-prefs" value="$${external_codec_prefs}"/>
    <param name="outbound-codec-prefs" value="$${external_codec_prefs}"/>
    <param name="inbound-codec-negotiation" value="generous"/>

    <param name="ext-rtp-ip" value="$${external_rtp_ip}"/>
    <param name="ext-sip-ip" value="$${external_sip_ip}"/>
    <param name="rtp-ip" value="$${local_ip_v4}"/>
    <param name="rtp-timer-name" value="soft"/>
    <param name="dtmf-type" value="rfc2833"/>
    <param name="manage-presence" value="false"/>
    <param name="rtp-secure-media" value="false"/>

    <param name="caller-id-in-from" value="true"/>
    <param name="username" value="freeswitch"/>
    <param name="password" value=""/>
  </settings>
</profile>
