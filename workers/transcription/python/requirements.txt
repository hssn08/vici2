# N07 Python GPU sidecar dependencies
# N07 PLAN §11.1

# Whisper transcription
faster-whisper>=1.0.3
whisperx>=3.1.5

# Audio I/O
soundfile>=0.12.1
torchaudio>=2.2.0; sys_platform != "win32"

# PII redaction
presidio-analyzer>=2.2.355
presidio-anonymizer>=2.2.355
spacy>=3.7.4

# HTTP server
fastapi>=0.111.0
uvicorn[standard]>=0.30.0
pydantic>=2.7.0

# AWS SDK (for presigned URL generation if needed)
boto3>=1.34.0

# Dev / testing only (not in production image)
pytest>=8.2.0
pytest-asyncio>=0.23.6
httpx>=0.27.0
numpy>=1.26.0
