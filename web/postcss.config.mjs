// Tailwind v4 (Oxide engine) via PostCSS plugin. No autoprefixer / postcss-import
// needed — Lightning CSS inside @tailwindcss/postcss handles both.
export default {
  plugins: {
    "@tailwindcss/postcss": {},
  },
};
