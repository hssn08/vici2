# Module M07 — Pause Codes + Statuses + Scripts

| Field | Value |
|---|---|
| Track | Admin UI |
| Phase | 1 |
| Effort | 3 days |
| Owner agent type | frontend |
| Status | NOT_STARTED |

## Goal
Admin UI for pause_codes, statuses (per campaign + global), and scripts (rich text with `{{lead.field}}` placeholders).

## Dependencies
- D04, M01, F02 (`scripts`, `pause_codes`)

## Public interface
**Pages:** `(admin)/pause-codes`, `(admin)/statuses`, `(admin)/scripts`.

## Plan phase
**Deliverable:** `spec/modules/M07/PLAN.md`: forms; rich-text editor pick (Tiptap); placeholder helper.

## Implementation phase
```
web/app/(admin)/pause-codes/page.tsx
web/app/(admin)/statuses/page.tsx
web/app/(admin)/scripts/page.tsx
web/app/(admin)/scripts/[id]/page.tsx
web/components/admin/scripts/ScriptEditor.tsx       # Tiptap-based
web/components/admin/scripts/PlaceholderMenu.tsx
api/src/routes/admin/scripts/*.ts
api/src/routes/admin/pause-codes/*.ts
```

## Verification phase
1. Add custom pause code "TRAINING"; appears in agent UI A09.
2. Add disposition "QUOTE_SENT" to campaign; appears in A06 picker.
3. Create script with `Hello {{lead.first_name}}, calling about...`; renders in agent UI A05 with substitution.
4. Cannot delete system-required statuses (NEW, DROP).

## Test phase
**Playwright.**

## Acceptance criteria
- [ ] CRUD for all three.
- [ ] Script editor + placeholder helper.
- [ ] Substitution renders in A05.
- [ ] System-required protected.
- [ ] Hotkey conflicts detected.

## Risks
- Trivial.

## Handoff documentation
**HANDOFF.md:** Editor extension; placeholder syntax `{{lead.first_name}}`, `{{call.uuid}}`, `{{agent.name}}`.
