# Module O05 — Security Baseline (TLS, Secrets, fail2ban)

| Field | Value |
|---|---|
| Track | Operations |
| Phase | 1 |
| Effort | 4–6 days |
| Owner agent type | sre / security |
| Status | NOT_STARTED |

## Goal
TLS everywhere (LE certs for WSS + HTTPS), fail2ban on FS + SSH, secrets in env (not committed), Docker image scanning, OWASP top-10 baseline check, dependency audit, KEK rotation procedure documented.

## Dependencies
- F01, F03, F05

## Public interface
- TLS termination strategy (LE auto-renew via certbot or caddy)
- fail2ban jails for SIP / SSH / failed-login
- Trivy / Snyk image scan in CI
- `docs/security/threat-model.md`

## Plan phase
**Deliverable:** `spec/modules/O05/PLAN.md`: TLS topology; fail2ban rules; scan config; threat model; pen-test plan.

## Implementation phase
```
deploy/caddy/Caddyfile.example
deploy/fail2ban/jails/{ssh,sip,nginx}.conf
.github/workflows/security-scan.yml
docs/security/threat-model.md
docs/security/incident-response.md
spec/runbooks/security-incident.md
```

## Verification phase
1. Real LE cert on staging.
2. Failed-login attack on SSH triggers ban.
3. SIP scan attack triggers ban.
4. Image scan in CI flags known CVEs.
5. Dependency audit clean (or annotated waivers).

## Acceptance criteria
- [ ] TLS everywhere.
- [ ] fail2ban active.
- [ ] CI security gates.
- [ ] Threat model + IR doc.
- [ ] KEK rotation runbook.
- [ ] Pen-test scheduled before serving regulated industries.

## Risks
- **Cert auto-renew silently fails** — alert on cert expiry < 14 days.

## Handoff documentation
**HANDOFF.md:** Cert renewal; jail rules; how to add a new scan.
