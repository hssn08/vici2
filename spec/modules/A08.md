# Module A08 — Callback Scheduling UI

| Field | Value |
|---|---|
| Track | Agent UI |
| Phase | 1 |
| Effort | 2 days |
| Owner agent type | frontend |
| Status | NOT_STARTED |

## Goal
UI for scheduling, viewing, snoozing, canceling callbacks. Agent sees their own; supervisor/admin see all.

## Dependencies
- D06 (callbacks API)

## Blocks
- M02 (campaign scheduled-callbacks toggle uses this)

## Public interface
- `<CallbackPicker leadId open onSubmit />` (modal triggered from dispo screen)
- `(agent)/callbacks` page lists pending
- Notification component (toast or sidebar) when due callback arrives via WS

## Research phase
- Date/time picker library (react-day-picker + custom time input).
- Time-zone display: agent's local tz; remote-party tz noted.

## Plan phase
**Deliverable:** `spec/modules/A08/PLAN.md`: component contracts; notification UX.

## Implementation phase
```
web/components/call/CallbackPicker.tsx
web/components/call/CallbackList.tsx
web/components/call/CallbackToast.tsx
web/app/(agent)/callbacks/page.tsx
web/test/callback.spec.ts
```

## Verification phase
1. From dispo screen, schedule callback in 5 min for self; pending list shows it.
2. Toast appears when due; clicking dials lead.
3. Snooze 30 min works; appears at new time.
4. Cancel removes from list.

## Test phase
**E2E (Playwright)** for the round-trip.
**Run:** `cd web && npm run test:e2e -- callback`

## Acceptance criteria
- [ ] Schedule, list, snooze, cancel.
- [ ] Notification on due (via WS).
- [ ] Time displayed in agent's tz.
- [ ] Validation: callback_at must be ≥ 1 min in future.

## Risks
- **Multi-tab notification dedup** — handled by A03 multi-tab strategy.

## Handoff documentation
**HANDOFF.md:** Component API; notification flow; where M02 surfaces the campaign toggle.
