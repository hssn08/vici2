# Module I01 — In-Groups (mod_callcenter)

| Field | Value |
|---|---|
| Track | Inbound |
| Phase | 3 |
| Effort | 5–6 days |
| Owner agent type | telephony + backend-node |
| Status | NOT_STARTED |

## Goal
Inbound queue support via FreeSWITCH `mod_callcenter`: queues with ring strategies (longest-idle, ring-all, round-robin, fewest-calls), agent skill/rank, MoH, max-queue, no-agent-action (voicemail/overflow). Templated config + admin UI.

## Dependencies
- F03 (mod_callcenter loaded), F02 (`ingroups`, `ingroup_agents`)

## Blocks
- I02 (DID routes to in-groups), I03 (IVR), I04 (closer), S01 (wallboard queue depth), A07 (LOCAL_CLOSER target)

## Public interface
**REST:** `/api/admin/ingroups` CRUD; agent assign/unassign.
**FS config:** `freeswitch/conf/autoload_configs/callcenter.conf.xml.tmpl` rendered from DB; reload via ESL.

## Research phase
**MCPs:**
- `mcp__exa__crawling_exa`: FS mod_callcenter docs (already cited)
- `mcp__exa__web_search_exa`: "FreeSWITCH callcenter agent tier rank skill production"

## Plan phase
**Deliverable:** `spec/modules/I01/PLAN.md`: template structure; reload flow; agent state mapping (callcenter agent state ↔ our agent state).

## Implementation phase
```
api/src/services/ingroups/ingroup-service.ts
api/src/services/ingroups/cc-renderer.ts            # writes callcenter.conf.xml
api/src/routes/admin/ingroups/*.ts
freeswitch/conf/dialplan/default/60_ingroup.xml
freeswitch/conf/autoload_configs/callcenter.conf.xml.tmpl
api/test/ingroups/*.test.ts
```

## Verification phase
1. Create in-group "SUPPORT"; assign 3 agents.
2. Inbound call routed to ingroup; lands with longest-idle agent.
3. Max-queue exceeded → caller routed to voicemail.
4. Agent log into queue (auto on agent READY); leaves on PAUSE.
5. Reload after config change without dropping live calls.

## Test phase
**Integration with SIPp** as inbound caller; multiple agents.

## Acceptance criteria
- [ ] CRUD; multi-strategy.
- [ ] Agent assign/unassign.
- [ ] Reload safe.
- [ ] Queue-depth metric per ingroup.
- [ ] Audit log.

## Risks
- **Mapping callcenter agent state ↔ our state** — keep them in sync via ESL events.

## Handoff documentation
**HANDOFF.md:** API; how I02 routes DIDs; how A07 transfers to ingroup.
