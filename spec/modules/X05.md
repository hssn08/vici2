# Module X05 — Local-Presence Caller-ID

| Field | Value |
|---|---|
| Track | Scale-out / Reputation |
| Phase | 3.5 |
| Effort | 3–4 days |
| Owner agent type | backend-node |
| Status | NOT_STARTED |

## Goal
Match called party's area code with a same-area-code DID from the pool to boost answer rates 30–60%. Falls back to general pool if no match.

## Dependencies
- X04 (number pool), D03 (resolve called area)

## Public interface
- Extends `pickCallerId()` to consider lead.area_code.

## Plan phase
**Deliverable:** `spec/modules/X05/PLAN.md`: matching algo; fallback chain.

## Implementation phase
```
api/src/services/number-pool/local-presence.ts
api/test/local-presence/*.test.ts
```

## Verification phase
1. Lead in area code 415 + pool has 415 DID → that DID used.
2. No matching DID → fall back to general pool.
3. Per-area-code metric: how often we matched.

## Acceptance criteria
- [ ] Matching algo correct.
- [ ] Fallback chain.
- [ ] Metric.

## Risks
- **DID inventory** must be broad to be useful — note in admin UI to recommend purchasing diverse DIDs.

## Handoff documentation
**HANDOFF.md:** Matching algo; how to extend to international.
