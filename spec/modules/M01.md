# Module M01 — Admin Next.js Skeleton + RBAC Routing

| Field | Value |
|---|---|
| Track | Admin UI |
| Phase | 1 |
| Effort | 3–4 days |
| Owner agent type | frontend |
| Status | NOT_STARTED |

## Goal
Admin shell layout: sidebar with navigation (Campaigns, Lists, Carriers, DIDs, Users, DNC, Statuses, Reports, Recordings), top bar with tenant + user, role-aware route protection (admin / superadmin).

## Dependencies
- A01 (auth + route groups)

## Blocks
- M02–M08, S01–S04, R03

## Public interface
- `(admin)/layout.tsx` with `<AdminSidebar />` + `<AdminTopBar />`
- Section pages stub at every route
- `useRequireRole('admin')` hook

## Research phase
- Tailwind dashboard layout patterns; existing react-admin alternatives (we're rolling our own to keep stack tight).

## Plan phase
**Deliverable:** `spec/modules/M01/PLAN.md`: nav structure; permission matrix; visual mockup.

## Implementation phase
```
web/app/(admin)/layout.tsx
web/app/(admin)/page.tsx                    # admin home / dashboard
web/components/admin/AdminSidebar.tsx
web/components/admin/AdminTopBar.tsx
web/components/admin/PageHeader.tsx
web/components/admin/DataTable.tsx          # reusable
web/components/admin/Form.tsx               # reusable
web/test/admin-shell.spec.ts
```

## Verification phase
1. Login as admin → admin home renders with nav.
2. Login as agent → cannot access `/(admin)/...` (redirect).
3. Sidebar nav highlights current section.
4. Mobile: sidebar collapses to hamburger.

## Test phase
**Playwright** for nav + RBAC.

## Acceptance criteria
- [ ] Layout + nav.
- [ ] RBAC enforced.
- [ ] Reusable DataTable + Form components for downstream M*.
- [ ] Mobile responsive.

## Risks
- Trivial.

## Handoff documentation
**HANDOFF.md:** Layout components; how M02–M08 add a new section to nav; DataTable/Form contract.
